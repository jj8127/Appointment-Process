# AGENTS.md

## Project Context & Operations

### Business Goal
- Build and operate a dual-platform FC onboarding system for insurance agents.
- Keep FC onboarding workflow and admin operations consistent across mobile app, admin web, and backend services.
- Protect sensitive identity data while preserving existing API and workflow behavior.

### Domain & Role Contract (GaramPA)
- `가람PA지사`는 FC(보험설계사) 조직이며, 고객 보험을 여러 보험사와 비교해 최적 설계를 제공한다.
- `가람in`은 앱 이름이며, 회사명/소속명(`company_name`, `affiliation`)으로 사용하지 않는다.
- 역할 경계(업무 기준):
  - `fc`: 고객 상담 후 보험사 설계 매니저에게 설계 의뢰를 요청하는 주체
  - `manager`(본부장): FC 리더 역할이며 request_board 의뢰 요청 주체 기준에서는 FC와 동일하게 취급
  - `admin`(총무): 가람PA지사 운영/행정 지원 역할, 설계 의뢰 요청 주체가 아님
  - `designer`(설계 매니저): 보험사 측 설계 담당자로 FC/본부장의 의뢰를 수신/처리
- 프로젝트 경계:
  - `fc-onboarding-app`: 위촉/온보딩/운영(FC·본부장·총무) 중심 시스템
  - `request_board`: FC(본부장 포함) -> 설계 매니저 설계의뢰 처리 시스템

### Tech Stack
- Mobile app: Expo (React Native, Expo Router, TanStack Query)
- Admin web: Next.js App Router (Mantine, TanStack Query)
- Backend: Supabase Postgres, Storage, Edge Functions (Deno)
- Shared language/runtime: TypeScript, Node.js

### Operational Commands
```bash
# Root (mobile app and shared code)
npm install
npm start
npm run android
npm run ios
npm run eas:build:android
npm run eas:build:ios
npm run lint
npm test
npm run test:coverage
```

```bash
# Admin web
cd web
npm install
npm run dev
npm run build
npm run lint
```

```bash
# Supabase
supabase login
supabase link --project-ref <project-ref>
supabase db push
supabase functions deploy <function-name> --project-ref <project-ref>
supabase secrets list --project-ref <project-ref>
```

## Current Status & Roadmap

### Snapshot (2026-02-16)
- Project is operating as a dual-platform monorepo: Expo mobile app + Next.js admin web + Supabase backend.
- Core FC onboarding flow is implemented end-to-end: signup, OTP/password, identity gate, consent, exam, docs, appointment, completion.
- FC signup supports dual-commission completion selection (`none/life_only/nonlife_only/both`); partial completion (`life_only/nonlife_only`) now starts from Step 1 (`draft`) and preserves completion flags so remaining track proceeds through full onboarding flow.
- Manager read-only mode is implemented and must stay enforced across dashboard, exam, notice, and chat areas.
- Auth is custom (phone + password via Edge Functions) and session state is based on `use-session`, not Supabase Auth session.
- Sensitive identity flow is established: encrypted resident number storage and service-role mediated access path.
- Recent stabilization work (2026-02-10 to 2026-02-11) focused on RLS-safe server APIs, notifications/notices reliability, exam deadline handling, and release hygiene cleanup.

### Canonical Workflow State
- User roles
  - `fc`: signup, identity, consent, exam apply, docs upload, appointment execution
  - `admin`: status updates, approvals, notices/notifications, exam operations
  - `manager`: read-only monitoring on web (no write actions)
- FC status values (source of truth: `types/fc.ts`)
  - `draft`
  - `temp-id-issued`
  - `allowance-pending`
  - `allowance-consented`
  - `docs-requested`
  - `docs-pending`
  - `docs-submitted`
  - `docs-rejected`
  - `docs-approved`
  - `appointment-completed`
  - `final-link-sent`
- FC commission completion flags (source of truth: `fc_profiles`)
  - `life_commission_completed`
  - `nonlife_commission_completed`
- Primary business flow
  - Signup and OTP/password set
  - Identity verification and basic info
  - Temp ID issue
  - Allowance consent and approval
  - Exam schedule/apply
  - Docs request/upload/review
  - Appointment schedule/completion
  - Final completion

### Bottom Navigation Contract (Mobile)
- Source of truth
  - `components/BottomNavigation.tsx` (UI/preset items)
  - `lib/bottom-navigation.ts` (`resolveBottomNavPreset`, `resolveBottomNavActiveKey`)
- Rule
  - 화면별 하단 네비게이션 프리셋은 반드시 `resolveBottomNavPreset`으로 계산한다.
  - `activeKey`는 반드시 `resolveBottomNavActiveKey`로 보정한다.
  - 수동 `Pressable` 하단바 구현/화면별 role ternary 복붙을 금지한다.
- Mapping invariants
  - `hydrated=false`이면 네비게이션을 렌더링하지 않는다(`null` preset).
  - `isRequestBoardDesigner=true`가 항상 최우선(`request-board-designer`).
  - `role=admin && readOnly=true`는 항상 `manager` 프리셋으로 고정.
  - `role=admin && readOnly=false`만 `admin-onboarding/admin-exam` 프리셋을 사용.
- Required tab sets (고정)
  - `admin-onboarding` / `admin-exam` / `manager`: `위촉 -> 시험 -> 설계요청 -> 게시판 -> 설정`
  - `request-board-designer`: `설계요청 -> 설정`
  - `fc`: `홈 -> 게시판 -> 설계요청 -> 설정`

### Active Roadmap
1. Backend and security hardening
- Keep admin write paths on trusted server/Edge Function routes only.
- Continue tightening CORS/origin/env validation and response-contract stability.
- Keep schema and migrations synchronized for every DB change.

2. Workflow consistency and regression prevention
- Align status transitions across mobile app, web dashboard, and Edge Functions.
- Prevent regressions in identity gate, docs approval/rejection, and appointment transitions.
- Expand targeted tests around status branching and authorization behavior.

3. Admin and manager UX reliability
- Preserve manager read-only behavior with explicit disabled controls in all write-capable views.
- Improve error visibility and fallback behavior for notice/chat/notification flows.
- Keep query invalidation and refresh paths deterministic after mutations.

4. Release and operations readiness
- Maintain Android/iOS release settings and deployment command accuracy.
- Keep governance docs and code changes synchronized in every functional PR.
- Maintain clean repository state by avoiding committed build artifacts and sensitive local configs.

### Working Backlog (Track in This File)
- In progress
  - Execute integrated checklist with zero-miss tracking (`docs/testing/INTEGRATED_TEST_CHECKLIST.md`, `integrated-test-cases.json`, `INTEGRATED_TEST_RUN_RESULT.json`) and keep `qa:validate:integrated` passing before sign-off.
  - Validate request-board 설계요청 탭 의뢰현황 통계카드(FC 4카드/설계매니저 4카드) renders correctly on-device and FC 설계코드 관리 화면 CRUD (add/edit/delete + company picker + missing companies alert) works end-to-end after bridge login (applied 2026-02-26).
  - Validate dual-commission signup branch on-device: `none/life_only/nonlife_only/both` each lands at intended home step and life/nonlife status badges (`완료/대기`) render correctly.
  - Re-verify manager login classification for request_board bridge (`manager` app role -> `fc` bridge role, not `designer`) on Android/iOS after OTA/app update rollout (server deploy + API retest completed on 2026-02-25).
  - Validate request-board mobile messenger attachment flow on-device (image/document picker -> upload -> message render/preview/download) after UI integration completed on 2026-02-25.
  - Separate mobile messenger identity between admin and manager sessions (manager uses personal phone-based chat actor id), scope manager chat list to assigned affiliation FCs, and render FC-side chat counterpart label as `{소속} {본부장 이름}` via affiliation/manager-account resolution (applied on 2026-02-25).
  - Route mobile notice-list card taps for unified board-notice items to board-screen detail modal (`/board?postId=`) so notice-opened posts and board-opened posts share identical UI (legacy notice-detail fallback and nested attachment/delete tap isolation maintained) applied on 2026-02-25.
  - Remove mobile bottom-navigation notice tab after notice/board unification (shared preset + home/board custom nav notice-item removal applied on 2026-02-25).
  - Harden FC mobile messenger live-sync reliability (insert immediate local reflect + realtime fallback poll + app-active refresh applied on 2026-02-25).
  - Stabilize admin web messenger live-update reliability (message send immediate render + room poll/focus refresh fallback + realtime refetch hardening applied on 2026-02-25).
  - Unify board `공지` category handling with notice-page feed/source so board notice posts are treated as notices in mobile notice list/latest notice paths (fc-notify merged notice sources + board-notice delete path applied on 2026-02-25).
  - Harden local EAS build path against Git clone-protection + local Husky hooksPath conflicts (wrapper commands + guarded prepare applied on 2026-02-25).
  - Normalize admin mobile bottom-navigation behavior across home/notice/board screens (route consistency + tab-count consistency hardening applied on 2026-02-25).
  - Stabilize admin mobile messenger peer-name rendering for FC-originated chat notifications (targetName fallback + profile-name resolution hardening applied on 2026-02-25).
  - Harden admin write paths to be consistently server-mediated (profile detail info/memo save path migrated on 2026-02-19).
  - Guard status transition consistency between mobile, web, and Edge Functions.
  - Add focused regression coverage for docs and appointment branching.
  - Expand notice/notification fallback and failure visibility (admin web push callback auth fallback + non-2xx logging hardened on 2026-02-24).
  - Stabilize mobile board UX/regressions after link-select text rendering + mutation completion routing hardening (image-only attachment reorder handle applied on 2026-02-24).
- Next
  - Migrate remaining direct client write paths on web screens to server-mediated admin APIs.
  - Add mobile notification-center regression coverage for long-press multi-select (tap/drag/scroll interaction paths).
  - Verify manager read-only behavior on all new dashboard actions.
  - Normalize date/deadline handling rules across UI and backend.
  - Re-verify Android/iOS device flow for board create/edit/delete completion and link option UX in post/comment text.
- Blockers/Risks
  - Re-enabling local Husky hooksPath (`core.hooksPath=.husky/_`) can reintroduce EAS file-clone upload failures unless wrapper build commands are used.
  - RLS and service-role path mismatch can silently break admin writes.
  - Schema-to-migration drift can cause deployment failures.
  - Status condition drift across app/web/function layers can cause UX inconsistencies.
  - Android FlatList + gesture-responder conflicts can regress drag-select behavior without device-level checks.
  - Browser/OS notification policy (Windows app-level block, Focus mode, Chrome quiet UI) can suppress desktop alerts even when web push send result is successful.

### Progress Recording Protocol
- `AGENTS.md` is the primary quick-status file for current progress and roadmap.
- Update this section whenever work changes delivery status, risks, or roadmap priorities.
- Add new entries at the top of the ledger and keep chronological readability.
- Entry format:
  - `YYYY-MM-DD | Scope | Change | Key Files | Verification | Next`
- If details are large, keep one-line summary here and reference deeper docs as optional support.

### Session Start Checklist
- Read `Current Status & Roadmap` and `Progress Ledger` in this file.
- Verify latest commits: `git log --oneline -10`.
- Check changed files and active branch before editing.
- Review similar implementation patterns in nearby modules before coding.

### Session Close Checklist
1. Add a new top entry in `Progress Ledger` with changed files and verification method.
2. Update `Working Backlog` statuses (move completed tasks, add new risks/next items).
3. If status model or rules changed, update `Snapshot` and `Canonical Workflow State`.
4. If schema changed, verify both `supabase/schema.sql` and `supabase/migrations/*.sql` were updated.
5. If CI/governance assumptions changed, update this file in the same change set.
6. Before every push, run `node scripts/ci/check-governance.mjs` locally and push only after it passes.

### Governance Validation Rules
- Documentation and schema governance checks are enforced in CI:
  - `.github/workflows/governance-check.yml`
  - `.github/pull_request_template.md`
- Required consistency checks:
  - docs and code changes stay synchronized for behavior-impacting work
  - schema and migrations stay synchronized for DB changes
  - note: CI currently treats any `supabase/migrations/*.sql` change as requiring a same-push `supabase/schema.sql` diff, even for data-fix/no-op migrations
  - auth/role behavior remains compatible with existing read-only and status constraints

### Push Preflight (Mandatory)
1. Run `node scripts/ci/check-governance.mjs` before commit and before push.
2. If `supabase/migrations/*.sql` changed, also include a `supabase/schema.sql` sync touch in the same push.
3. If code paths (`app/`, `web/`, `supabase/`, `components/`, `hooks/`, `lib/`, `types/`) changed, update both `.claude/WORK_LOG.md` and `.claude/WORK_DETAIL.md` together.
4. Re-run governance check; push only when output is `[governance-check] passed`.

### Progress Ledger
- `2026-02-27 | Affiliation→Manager Mapping Table for FC Update Notifications | Replaced manager-name parsing recipient resolution with explicit affiliation mapping table (`affiliation_manager_mappings`) so `fc_update/fc_delete` fanout targets managers by affiliation membership (supports multiple managers per affiliation) while keeping 총무 full-scope; added affiliation label normalization (legacy->team labels), migration + schema/RLS/policy sync, manager account delete cleanup for mapping rows, and operator SQL examples for mapping maintenance | supabase/migrations/20260227000008_add_affiliation_manager_mappings.sql, supabase/schema.sql, supabase/functions/fc-notify/index.ts, supabase/functions/delete-account/index.ts, docs/guides/COMMANDS.md, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | DB push pass (`supabase db push --linked`), migration parity pass (`supabase migration list --linked`), function deploy pass (`supabase functions deploy fc-notify --project-ref ubeginyxaotcamuqpmud`), governance pass (`node scripts/ci/check-governance.mjs`), recipient smoke pass (temp manager+mapping include verified then cleaned) | Maintain `affiliation_manager_mappings` as source of truth; add multiple rows per affiliation to fanout to all assigned managers`
- `2026-02-27 | Notification Inbox SSOT + Manager FC-Update Scope | Unified mobile inbox role contract across home/request-board/notifications to use session `role + residentId` consistently (removed request_board-role forced `fc` mapping), and hardened `fc-notify` admin inbox queries (`inbox_list/unread/delete`) to scope by `resident_id` when provided; changed `fc_update/fc_delete` fanout to targeted admin recipients so managers only receive updates for FCs whose `affiliation` maps to their manager account while 총무 remains full-scope | app/index.tsx, app/request-board.tsx, app/notifications.tsx, supabase/functions/fc-notify/index.ts, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- app/index.tsx app/request-board.tsx app/notifications.tsx`), edge-function lint note (`npm run lint -- supabase/functions/fc-notify/index.ts` shows expected Deno remote import unresolved) | Deploy `fc-notify` and validate on-device with manager/admin accounts that home/request-board bell counts match and manager only sees own-affiliation FC update notifications`
- `2026-02-27 | Notification Badge Rendering Fix (99 overflow) | Fixed top-header bell badge visual break by locking badge text scaling (`allowFontScaling=false`, `maxFontSizeMultiplier=1`), resizing badge geometry for 2/3-char counts, and setting icon-button overflow visible to prevent clipping | components/AppTopActionBar.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- components/AppTopActionBar.tsx`) | Verify on-device that unread counts (e.g., 99/99+) render inside a stable red badge`
- `2026-02-27 | Request Stats Card Drilldown to Filtered Request List | Wired request-board `의뢰 현황` cards to open existing `의뢰 목록·검토` screen with filter params (`all/pending/in_progress/completed`), and extended request-list screen to honor query-param bootstrap filter + added `수락 대기` tab while preserving existing `검토 대기` filter | app/request-board.tsx, app/request-board-requests.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- app/request-board.tsx app/request-board-requests.tsx`) | Verify on-device: each stats card opens request list with matching filter preselected`
- `2026-02-27 | Welcome Title Font Downsize (2nd pass) | Further reduced shared top-header welcome text size to improve readability and avoid oversized visual weight across home/request-board/board/admin-board-manage | components/AppTopActionBar.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- components/AppTopActionBar.tsx`) | Verify on-device that header greeting is visibly smaller and balanced with icon/logout controls`
- `2026-02-27 | Board View Count Semantics Change (Per-View Increment) | Switched board view counting from unique-viewer semantics to per-detail-open event semantics by removing `(post_id,resident_id)` unique constraint on `board_post_views` and changing detail tracking from `upsert` to `insert`; updated stats views so `view_count` reflects accumulated view events | supabase/functions/board-detail/index.ts, supabase/schema.sql, supabase/migrations/20260227000007_allow_repeated_board_views.sql, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | governance pass (`node scripts/ci/check-governance.mjs`) | Apply migration and verify repeated opens by same account increase view count each time`
- `2026-02-27 | Header Title Size Tuning + Admin Board Header Unification | Reduced oversized welcome-title typography in shared top action bar (to prevent truncation) and migrated `/admin-board-manage` to the same header contract (`알림-환영문구-로그아웃`) used by home/request-board/board; removed screen-local refresh header on admin board manage and kept section title below the shared header | components/AppTopActionBar.tsx, app/admin-board-manage.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- components/AppTopActionBar.tsx app/admin-board-manage.tsx app/board.tsx`) | Verify on-device that welcome text no longer appears oversized/truncated and board manage header matches other pages`
- `2026-02-27 | Header One-Row Layout (Bell-Title-Logout) | Replaced previous two-row top actions with a single-row contract: left notification button, centered welcome title, right logout button; removed settings action and disabled native stack header on home/request-board/board to prevent duplicate greeting text | components/AppTopActionBar.tsx, app/index.tsx, app/request-board.tsx, app/board.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- components/AppTopActionBar.tsx app/index.tsx app/board.tsx app/request-board.tsx`) | Verify on-device that home/request-board/board all render identical top row and no settings icon appears`
- `2026-02-27 | Board Category-Title Inline Alignment | Refined board UI after feedback so category badge and title now start on the same row (no forced line break) in mobile board-manage list/detail and board list cards; added inline badge/title styles to avoid margin-wrap drift | app/admin-board-manage.tsx, app/board.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- app/admin-board-manage.tsx app/board.tsx`) | Verify on-device that category badge and title begin on one line in both board surfaces`
- `2026-02-27 | Board Category Priority + Per-Post View Tracking | Updated mobile board-manage/detail presentation so category badge (including notice posts) is shown before title, added per-post unique viewer tracking (`board_post_views`) and surfaced view counts in app board list/detail + board-manage list/detail; synchronized board API contracts/types and resident cleanup paths for account deletion | app/admin-board-manage.tsx, app/board.tsx, supabase/functions/board-detail/index.ts, supabase/functions/board-list/index.ts, supabase/schema.sql, supabase/migrations/20260227000006_add_board_post_views.sql, lib/board-api.ts, web/src/lib/board-api.ts, contracts/api-contracts.md, supabase/functions/delete-account/index.ts, supabase/functions/admin-action/index.ts, web/src/app/api/fc-delete/route.ts, web/src/app/dashboard/board/page.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- app/admin-board-manage.tsx app/board.tsx lib/board-api.ts`; `cd web && npm run lint -- src/lib/board-api.ts src/app/dashboard/board/page.tsx src/app/api/fc-delete/route.ts`), governance pass (`node scripts/ci/check-governance.mjs`) | Verify on-device that board-manage card/modal both show `eye` view count and category badge appears before post title`
- `2026-02-27 | Section Title Alignment (Onboarding/Exam/RequestBoard) | Applied centered alignment for admin home section title/subtitle (`위촉`/`시험`) and request-board section title/subtitle (`설계 요청`) to match requested visual composition | app/index.tsx, app/request-board.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- app/index.tsx app/request-board.tsx`) | Verify on Android/iOS that all three title groups render centered across device widths`
- `2026-02-27 | Header Bell Badge Contract Unification (Home vs RequestBoard) | Fixed inconsistent bell-badge clear behavior by switching request-board header badge source from list length to shared unread-count contract (`fc-notify` `inbox_unread_count` + `lastNotificationCheckTime`), and added focus-time refetch so returning from notifications immediately clears badge when read | app/request-board.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- app/request-board.tsx`) | Verify on-device: read notifications -> return to request-board -> bell badge clears identically to home`
- `2026-02-27 | Designer Welcome Title Contract | Extended shared welcome-title resolver so request_board designer sessions render role-specific greeting (`{이름} 설계 매니저님 환영합니다.`) with suffix de-dup fallback, then propagated the resolver input to home/request-board/board screens | lib/welcome-title.ts, app/index.tsx, app/request-board.tsx, app/board.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- lib/welcome-title.ts app/index.tsx app/request-board.tsx app/board.tsx`) | Verify with real designer session that native header title shows designer greeting on landing screens`
- `2026-02-27 | Exam Round Delete Interaction Fix (Life/Nonlife) | Fixed mobile exam schedule delete button non-response caused by nested `Pressable` event capture: added `stopPropagation()` on delete CTA inside round cards for both life and nonlife register screens, and added disabled visual state for readOnly sessions to prevent false-negative tap expectations | app/exam-register.tsx, app/exam-register2.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | lint pass (`npm run lint -- app/exam-register.tsx app/exam-register2.tsx`) | Verify on Android/iOS that tapping `삭제` opens confirmation dialog and does not only select the round card`
- `2026-02-27 | Mobile Top Header Standardization (Home/RequestBoard/Board) | Removed per-screen header drift by introducing shared `AppTopActionBar` and shared welcome-title resolver: deleted right refresh action, moved settings action to the right side, and applied identical top action layout to `index`(위촉/시험), `request-board`(설계요청), and `board`(게시판); request-board/board now also use the same welcome-title header contract as home | components/AppTopActionBar.tsx, lib/welcome-title.ts, app/index.tsx, app/request-board.tsx, app/board.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md | lint pass (`npm run lint -- app/index.tsx app/request-board.tsx app/board.tsx components/AppTopActionBar.tsx lib/welcome-title.ts`), unit test pass (`npm test -- --runInBand lib/__tests__/bottom-navigation.test.ts`) | Verify on Android/iOS role matrix (FC/총무/본부장/설계매니저) that top action bar stays identical and no refresh button reappears`
- `2026-02-26 | Remaining BLOCKED Zero-Fail Closure | Closed all remaining integrated BLOCKED cases by fixing runner/runtime gaps: (1) request_board request-create payload updated with per-designer code selections + PDF messenger attachment upload contract, (2) delete-account expanded to support self-delete for admin/manager as well as FC (role-hint aware lookup + shared resident cleanup), (3) mobile/web settings account-delete UI unblocked for 본부장/총무 and role hint wired, (4) delete-account function deployed to Supabase and integrated run-result/evidence refreshed to all PASS | scripts/testing/run-remaining-blocked-cli.mjs, supabase/functions/delete-account/index.ts, app/settings.tsx, web/src/app/dashboard/settings/page.tsx, contracts/api-contracts.md, docs/testing/INTEGRATED_TEST_RUN_RESULT.json, docs/testing/evidence/remaining-blocked-cli-2026-02-26T16-13-37-899Z.{json,md} | runner pass (`node scripts/testing/run-remaining-blocked-cli.mjs` => PASS 42/FAIL 0), integrated validator pass (`npm run qa:validate:integrated` => PASS 52/FAIL 0/BLOCKED 0), lint pass (`npm run lint -- app/settings.tsx scripts/testing/run-remaining-blocked-cli.mjs`, `cd web && npm run lint -- src/app/dashboard/settings/page.tsx`) | Keep using role hint for delete-account callers to avoid cross-role phone collision ambiguity and keep integrated evidence file updated per rerun`
- `2026-02-26 | Role-Ordered BLOCKED Execution (FC→본부장→총무→설계매니저) | Added executable CLI integration runners and converted previously blocked checklist cases to verified PASS where API-level end-to-end validation was feasible: FC commission/signup+request_board sync (`ONB-01`, `P0-11`, `P0-12`, `P0-13`), 본부장(manager) 설계코드 CRUD + readOnly API guard partial evidence (`RB-08`, `P0-02 partial`), 설계매니저 request lifecycle (`RB-03`, `RB-05`), 총무 allowance consent approve/reject/resubmit transition (`ONB-04`); all test users/data cleaned up after each run and integrated validator kept green | scripts/testing/run-fc-blocked-cli.mjs, scripts/testing/run-manager-blocked-cli.mjs, scripts/testing/run-designer-blocked-cli.mjs, scripts/testing/run-admin-blocked-cli.mjs, docs/testing/evidence/*.md, docs/testing/evidence/*.json, docs/testing/INTEGRATED_TEST_RUN_RESULT.json, AGENTS.md | runner executions pass (`node scripts/testing/run-*-blocked-cli.mjs`), integrated validator pass (`npm run qa:validate:integrated`), current tally `PASS=10, BLOCKED=42, FAIL=0` | Continue role-order execution on remaining BLOCKED items that need device/UI/manual cross-platform validation and keep adding evidence with cleanup discipline`
- `2026-02-26 | Integrated Test Execution Framework (Zero-Miss) | Converted the consolidated checklist into executable artifacts so no case can be skipped silently: added canonical 52-case catalog (`integrated-test-cases.json`) covering 위촉/게시판/설계요청/설정/기타 + P0/P1, added operator runbook (`INTEGRATED_TEST_CHECKLIST.md`), added run-result bootstrap script (`qa:init:integrated`) that materializes case rows, and added strict validator (`qa:validate:integrated`) that fails on missing/duplicate/invalid status/NOT_RUN and enforces evidence/notes requirements by status | docs/testing/INTEGRATED_TEST_CHECKLIST.md, docs/testing/integrated-test-cases.json, scripts/testing/init-integrated-test-run.mjs, scripts/testing/validate-integrated-test-run.mjs, package.json, AGENTS.md | run generation pass (`npm run qa:init:integrated -- --force`), validator gate behavior verified (`npm run qa:validate:integrated` fails when 52 cases are NOT_RUN), lint pass (`npm run lint -- docs/testing/INTEGRATED_TEST_CHECKLIST.md scripts/testing/init-integrated-test-run.mjs scripts/testing/validate-integrated-test-run.mjs`) | Fill run-result evidence/status on each case and keep validator green before release decision`
- `2026-02-26 | Bottom Nav Policy Correction (Admin/Manager 5 Tabs) | Corrected bottom-nav contract to match role UX requirements: both 총무(admin) and 본부장(manager/readOnly) now use 5 fixed tabs in identical order (`위촉 -> 시험 -> 설계요청 -> 게시판 -> 설정`), request-board designer remains 2 tabs (`설계요청 -> 설정`), and home screen now treats manager preset as admin-like so onboarding/exam tab active state and tab switching are deterministic | components/BottomNavigation.tsx, lib/bottom-navigation.ts, app/index.tsx, lib/__tests__/bottom-navigation.test.ts, AGENTS.md | lint pass (`npm run lint -- components/BottomNavigation.tsx lib/bottom-navigation.ts app/index.tsx lib/__tests__/bottom-navigation.test.ts`), unit test pass (`npm test -- --runInBand`) | Verify on Android/iOS with admin+manager accounts that all mobile screens keep the same 5-tab set and order`
- `2026-02-26 | Mobile Bottom Navigation SSOT Hardening (Admin/Manager/FC/Designer) | Fixed recurring bottom-nav drift where 총무/본부장 sessions saw different tab sets across screens by introducing a shared resolver contract (`resolveBottomNavPreset`, `resolveBottomNavActiveKey`) and applying it to all bottom-nav surfaces (`/`, `/board`, `/admin-board-manage`, `/notice`, `/settings`, `/request-board`, `/request-board-fc-codes`); replaced home/board manual nav rendering with shared `BottomNavigation`, enforced `readOnly admin -> manager preset`, and added hydration-safe null preset handling to prevent first-frame role mismatch flicker; added unit tests for preset/active-key mapping and documented non-regression rules in AGENTS | lib/bottom-navigation.ts, components/BottomNavigation.tsx, app/index.tsx, app/board.tsx, app/admin-board-manage.tsx, app/notice.tsx, app/settings.tsx, app/request-board.tsx, app/request-board-fc-codes.tsx, lib/__tests__/bottom-navigation.test.ts, AGENTS.md | mobile lint pass (`npm run lint -- components/BottomNavigation.tsx lib/bottom-navigation.ts app/index.tsx app/board.tsx app/admin-board-manage.tsx app/notice.tsx app/settings.tsx app/request-board.tsx app/request-board-fc-codes.tsx lib/__tests__/bottom-navigation.test.ts`), unit test pass (`npm test -- --runInBand`), governance pass (`node scripts/ci/check-governance.mjs`) | Run Android/iOS role matrix smoke test: admin, manager(readOnly), fc, request-board designer across home/board/request-board/settings/notice transitions`
- `2026-02-26 | Request-Board 설계요청 탭 의뢰현황 통계 + 설계코드 관리 화면 | Added request_board API stats integration and FC code management screen to mobile app: (1) added `rbGetRequests` to fetch request_board requests for stats computation, added `rbGetFcCodes/rbGetCompanyNames/rbCreateFcCode/rbUpdateFcCode/rbDeleteFcCode` APIs; (2) modified `request-board.tsx` to fetch requests in parallel with notifications and show 4-card stats grid (FC view: 전체의뢰/수락대기/진행중/완료, Designer view: 수락대기/작업중/이번달완료/평균처리시간) plus "설계코드 관리" pressable shortcut for FC users; (3) created new `request-board-fc-codes.tsx` screen with full CRUD (list + add/edit modal with company picker + delete confirm + missing-companies alert chip) | app/request-board.tsx, lib/request-board-api.ts, app/request-board-fc-codes.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/request-board.tsx lib/request-board-api.ts app/request-board-fc-codes.tsx`) | Validate on Android/iOS: FC sees 4-stat cards with real request_board counts, tap 설계코드 관리 navigates to code list, add/edit/delete CRUD works with bridge auth`
- `2026-02-26 | Partial-Commission Signup Step Regression Fix | Fixed onboarding regression where signup `life_only/nonlife_only` was stored as `appointment-completed` and caused immediate Step 4 jump/early lock paths: extracted commission mapping to shared edge-function helper, changed signup mapping to keep partial completion at `draft` with flags only, rewired mobile/web/admin `calcStep` to prioritize workflow gates (identity -> allowance -> docs -> appointment) over partial flags, updated appointment confirm status transitions to consider pre-completed commission flags (so flag+approved-date becomes `final-link-sent`), and added safety migration to demote legacy `appointment-completed` partial-signup rows to `draft` only when no onboarding progress exists | supabase/functions/_shared/commission.ts, supabase/functions/set-password/index.ts, app/index.tsx, app/dashboard.tsx, web/src/lib/shared.ts, supabase/functions/admin-action/index.ts, web/src/app/dashboard/appointment/actions.ts, web/src/app/dashboard/page.tsx, supabase/migrations/20260226000004_fix_partial_commission_signup_status.sql, lib/__tests__/commission.test.ts, lib/__tests__/workflow-step-regression.test.ts, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | mobile lint pass (`npm run lint -- app/index.tsx app/dashboard.tsx lib/__tests__/commission.test.ts lib/__tests__/workflow-step-regression.test.ts`), unit test pass (`npm test -- --runInBand`), web lint pass (`cd web && npm run lint -- src/lib/shared.ts src/app/dashboard/appointment/actions.ts src/app/dashboard/page.tsx`), web build pass (`cd web && npm run build`), risk simulation pass (`appointment status transition simulation: ok`, `migration demotion predicate simulation: ok`), prod migration pass (`supabase db push --linked`), prod function deploy pass (`set-password`, `admin-action`), runtime smoke pass (`set-password invalid_phone`, `admin-action unauthorized`), remote data integrity pass (`partialAppointmentCompleted=0`, `bothDoneNotFinal=0`), prod web deploy pass (`vercel --prod --archive=tgz`) | Execute Android/iOS manual signup verification for `none/life_only/nonlife_only/both``
- `2026-02-26 | Admin Web Header Notification Center + Sidebar Simplification | Removed dashboard sidebar `알림/공지` nav item and added header bell dropdown notification center for admin web. New bell menu loads unified inbox (`fc-notify` `inbox_list`: notifications + notices), shows source/category badges, supports route normalization to relevant dashboard pages on click, and tracks per-user checked state in browser storage so unread badge count decreases immediately when items are confirmed. Included refresh and `모두 확인` actions in the bell dropdown | web/src/components/DashboardNotificationBell.tsx, web/src/app/dashboard/layout.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | web lint pass (`cd web && npm run lint -- src/app/dashboard/layout.tsx src/components/DashboardNotificationBell.tsx`), web build pass (`cd web && npm run build`), governance pass (`node scripts/ci/check-governance.mjs`) | Verify in production admin browser that bell-click navigation matches each notification target and unread badge decrements on item confirm`
- `2026-02-26 | FC Internal Messenger Target Picker Recovery | Fixed FC-side 가람지사 메신저 failure where manager targets disappeared and 총무 entry could not open chat: moved FC target resolution from direct anon `manager_accounts` query to service-role mediated `fc-notify` (`type: chat_targets`) endpoint, added FC target picker UI (`본부장` + `총무`) in mobile chat entry, and corrected `targetId=admin` normalization so 총무 chat no longer collapses to empty receiver id | app/chat.tsx, supabase/functions/fc-notify/index.ts, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | mobile lint pass (`npm run lint -- app/chat.tsx app/messenger.tsx app/admin-messenger.tsx`), unit test pass (`npm test -- --runInBand`), governance pass (`node scripts/ci/check-governance.mjs`), deploy pass (`supabase functions deploy fc-notify --project-ref ubeginyxaotcamuqpmud`), runtime probe pass (`fc-notify chat_targets resident_id=01064122836 => managers[서선미/01093118127]`) | Validate on Android/iOS that FC entering `/messenger -> 가람지사 메신저` sees all 본부장 targets and can open/send to both 본부장 and 총무`
- `2026-02-26 | Mobile Admin Board Category Badge Visibility | Added category visibility to app `게시판 관리` list cards by fetching board categories alongside post list, mapping `categoryId -> categoryName`, and rendering per-post category badges (`공지/교육/서류/일반`) with category-specific color themes so operators can identify post type without opening detail | app/admin-board-manage.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | mobile lint pass (`npm run lint -- app/admin-board-manage.tsx`) | Verify on device that category badges appear for all board posts and fallback label is stable for unknown category IDs`
- `2026-02-26 | Admin Web Notice/Board Notice Unification | Unified dashboard `알림/공지` source with board `공지` category by extending `/api/admin/notices` to merge legacy `notices` rows and `board_posts(slug=notice)` rows, supporting `board_notice:{postId}` IDs for list/detail/delete paths (with board attachment signed-url mapping and storage cleanup on delete), routing notifications list/detail/edit board-notice entries to board detail surface, and adding `postId` deep-link open/close handling in web board page so notice-open and board-open land on the same UI | web/src/app/api/admin/notices/route.ts, web/src/app/dashboard/notifications/page.tsx, web/src/app/dashboard/notifications/[id]/page.tsx, web/src/app/dashboard/notifications/[id]/edit/page.tsx, web/src/app/dashboard/board/page.tsx, .claude/WORK_LOG.md, .claude/WORK_DETAIL.md, AGENTS.md | web lint pass (`cd web && npm run lint -- src/app/api/admin/notices/route.ts src/app/dashboard/notifications/page.tsx src/app/dashboard/notifications/[id]/page.tsx src/app/dashboard/notifications/[id]/edit/page.tsx src/app/dashboard/board/page.tsx`), web build pass (`cd web && npm run build`) | Verify in production dashboard that board notice rows appear in notice list, open into board modal via `postId`, and manager/admin delete permission parity is preserved`
- `2026-02-25 | FC Dual-Commission Branching (Signup + Step Logic) | Added signup-time commission completion selection (`none/life_only/nonlife_only/both`) and persisted it through password setup into `fc_profiles` (`life_commission_completed`, `nonlife_commission_completed`, status mapping to `draft`/`appointment-completed`/`final-link-sent`); updated mobile home step priority so `final-link-sent` is always Step 5 and one-track completion maps to Step 4, changed home commission badges to explicit `완료/대기` with `위촉 완료 X/2`, aligned web/admin shared step logic (`calcStep`, appointment progress), and adjusted admin step grouping so pre-completed FCs are grouped into existing Step 3/4 buckets even when `identity_completed=false`; added schema+migration sync and prevented `/fc/new` profile edits from resetting status back to `draft` | app/signup.tsx, app/signup-verify.tsx, app/signup-password.tsx, app/index.tsx, app/fc/new.tsx, supabase/functions/set-password/index.ts, types/fc.ts, web/src/types/fc.ts, web/src/lib/shared.ts, web/src/app/dashboard/page.tsx, supabase/schema.sql, supabase/migrations/20260225000003_add_commission_completion_flags.sql, AGENTS.md | lint pass (`npm run lint -- app/signup.tsx app/signup-verify.tsx app/signup-password.tsx app/index.tsx app/fc/new.tsx types/fc.ts`), web lint pass (`cd web && npm run lint -- src/lib/shared.ts src/types/fc.ts src/app/dashboard/page.tsx`), governance pass (`node scripts/ci/check-governance.mjs`), deploy pass (`supabase functions deploy set-password --project-ref ubeginyxaotcamuqpmud`), migration pass (`supabase db push --linked`) with backfill/status normalization | Run on-device signup scenarios for all four commission states`
- `2026-02-25 | request_board Manager Role Alignment + Bridge Retest | Corrected manager(request_board 본부장/FC 리더) handling so mobile app no longer auto-marks manager as request_board designer: `isRequestBoardDesigner` is now enabled only when bridge-login returns `designer`, session stores explicit `requestBoardRole`, request_board/notifications inbox queries use bridge-role-aware `fc` lookup, and request-board header gained URL-copy action; deployed `login-with-password` edge function so manager bridge token role is `fc` and re-ran production API checks (`login-with-password` + `/api/auth/bridge-login` + DM->fc-notify inbox) confirming manager path now succeeds | hooks/use-login.ts, hooks/use-session.tsx, app/request-board.tsx, app/notifications.tsx, supabase/functions/login-with-password/index.ts, AGENTS.md | lint pass (`npm run lint -- hooks/use-login.ts hooks/use-session.tsx app/request-board.tsx app/notifications.tsx`), deploy pass (`supabase functions deploy login-with-password --project-ref ubeginyxaotcamuqpmud`), integration pass (manager bridge login 200 + request_board_message inbox reflection for `01093118127`) | Ship OTA/app build and run device-level UX verification for manager login -> request-board screen -> notifications`
- `2026-02-25 | request_board Auth/Password Integration | Integrated app login with request_board bridge auth: `login-with-password` now issues `requestBoardBridgeToken`, mobile request-board messenger removed standalone request_board password login and now uses app-session auto bridge login (+401 auto-retry), and password-change functions (`set-password`, `reset-password`, `set-admin-password`) now call request_board password sync API; production secrets aligned and cross-system password propagation verified | app/request-board-messenger.tsx, lib/request-board-api.ts, hooks/use-login.ts, hooks/use-session.tsx, supabase/functions/login-with-password/index.ts, supabase/functions/set-password/index.ts, supabase/functions/reset-password/index.ts, supabase/functions/set-admin-password/index.ts, AGENTS.md | lint pass (`npx eslint hooks/use-login.ts hooks/use-session.tsx lib/request-board-api.ts app/request-board-messenger.tsx`), API integration check (`login-with-password -> /api/auth/bridge-login`, `reset-password -> /api/auth/sync-password`) | Push mobile app update/OTA and verify on-device that request-board messenger opens without extra login after app sign-in`
- `2026-02-25 | request_board Messenger Attachment UI (Mobile) | Implemented full file/image attachment support on mobile request_board messenger screen: picker actions (gallery/document), pending attachment strip with remove, upload-before-send flow via `rbUploadAttachments`, attachment-aware message rendering (image grid + file cards), and fullscreen image preview modal; tightened typings to remove implicit/unsafe anys in new render path | app/request-board-messenger.tsx | mobile lint pass (`npm run lint -- app/request-board-messenger.tsx`); type check scope pass (`npx tsc --noEmit --pretty false` with file-specific check shows no `request-board-messenger.tsx` errors) | Verify on Android/iOS for both request chat and DM: image/doc upload, attachment-only send, download/open link, and large-file failure feedback`
- `2026-02-25 | Mobile Messenger Admin/Manager Identity Split + FC Counterpart Labeling | Fixed messenger role collision where manager sessions were treated as the same chat actor as admin (`sender_id/receiver_id='admin'`) by switching manager sessions to personal phone-based chat actor id, scoping manager messenger list to FCs mapped to that manager from affiliation metadata, and resolving FC chat counterpart to manager account (`manager_accounts`) so FC chat header/peer label shows `{소속} {본부장 이름}` when available (fallback `총무팀`) | app/chat.tsx, app/admin-messenger.tsx, lib/messenger-participants.ts, app/index.tsx, app/home-lite.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/chat.tsx app/admin-messenger.tsx app/index.tsx app/home-lite.tsx lib/messenger-participants.ts`) | On Android/iOS: verify manager and admin write/read separate threads, manager sees only 담당 FC 목록, and FC chat header shows 소속+본부장명 for mapped affiliation`
- `2026-02-25 | Notification Source Separation (Onboarding vs request_board) | Clarified end-user distinction between two notification channels by (1) adding push-title source tagging for request-board-origin payloads (`[설계요청]` prefix on `request_board_*` categories) and source metadata in push data, and (2) updating mobile notification-center rendering to show source badges (`설계요청` / `온보딩앱`) plus normalized request-board category labels (`의뢰 수락/거절/완료`, `새 메시지` 등); request-board items now stay in-place with guidance alert instead of deep-linking to unrelated onboarding screens | supabase/functions/fc-notify/index.ts, app/notifications.tsx, AGENTS.md | deploy verified (`supabase functions deploy fc-notify --project-ref ubeginyxaotcamuqpmud`); lint pass (`npm run lint -- app/notifications.tsx`) | Push title source prefix is effective immediately, while in-app badge UI is reflected after mobile app build/update`
- `2026-02-25 | Notice->Board Detail UI Parity | Eliminated notice/opened-post UI mismatch by routing unified board notice cards to `/board?postId=<id>` and adding board-screen query-param deep-link handling that auto-opens the existing in-screen post-detail modal (with route-param cleanup on close), so notice entry now lands on the same UI surface as board list post open | app/notice.tsx, app/board.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/board.tsx app/notice.tsx`) | On Android/iOS: open a unified notice from notice list and confirm it appears in the same board detail modal UI as tapping the same post in board list`
- `2026-02-25 | fc-notify Role-Agnostic Target Delivery | Expanded `fc-notify` token resolution so when `target_id` is provided it sends to all tokens bound to that resident phone regardless of role (`fc/admin/manager`), and deduplicates by Expo token to prevent duplicate sends from multi-role rows; also aligned legacy `admin_update -> fc` path to resident-id based token lookup | supabase/functions/fc-notify/index.ts, AGENTS.md | deploy verified (`supabase functions deploy fc-notify --project-ref ubeginyxaotcamuqpmud`); live probe `POST https://adminweb-red.vercel.app/api/fc-notify` with `target_role=fc,target_id=01093118127` returns `sent:2` (previously `sent:0`) | Keep bridge payload contract stable (`target_id` phone digits) and monitor duplicate-token counts in production logs`
- `2026-02-25 | Notice List -> Board Detail Deep Link | Updated mobile notice page so tapping a notice card opens the corresponding board post detail when notice id is unified board notice (`board_notice:*`); retained fallback to legacy notice detail for historical rows and prevented card-level navigation from hijacking nested file/image/delete interactions via stopPropagation | app/notice.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/notice.tsx`) | Verify on device that notice cards open board detail directly for unified notices and attachments/delete actions still behave independently`
- `2026-02-25 | Mobile Bottom Nav Notice Tab Removal | Removed `공지` tab from mobile bottom navigation to match notice/board unification: deleted notice item from shared nav presets (fc/admin/manager), removed hardcoded notice button from home and board custom bottom-nav UIs, and aligned notice screen nav active key to remaining board tab | components/BottomNavigation.tsx, app/index.tsx, app/board.tsx, app/notice.tsx, AGENTS.md | mobile lint pass (`npm run lint -- components/BottomNavigation.tsx app/index.tsx app/board.tsx app/notice.tsx`) | Verify on device that bottom nav now has no notice tab across home/board/settings/notice surfaces and tab spacing remains stable`
- `2026-02-25 | FC Mobile Messenger Live Sync Hardening | Reviewed FC messenger real-time path and fixed refresh-dependent risk by adding hybrid sync in mobile `chat` screen: immediate local append after insert success, periodic room refetch fallback, and app foreground refresh trigger while preserving realtime subscription/update/delete handling and unread mark sync | app/chat.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/chat.tsx`) | On device, verify FC send/receive reflects without manual refresh even when realtime callback is delayed, and unread state on admin-originated messages is cleared while chat is open`
- `2026-02-25 | Admin Web Messenger Live Sync Reliability | Fixed dashboard messenger delayed-update behavior (messages visible only after manual refresh) by adding immediate local append on successful send, room-level short polling fallback, browser focus/visibility refresh trigger, and realtime callback hardening to refetch authoritative room data for related rows | web/src/app/dashboard/chat/page.tsx, AGENTS.md | web lint pass (`cd web && npm run lint -- src/app/dashboard/chat/page.tsx`); web build pass (`cd web && npm run build`) | Validate on two clients: admin sends -> own bubble appears instantly; FC replies -> admin room updates without page reload; left list unread/order updates within realtime/poll path`
- `2026-02-25 | Notice/Board Unification (Mobile Notice Feed) | Unified notice treatment so board posts created under category slug `notice` are now surfaced in `fc-notify` notice responses alongside legacy `notices` rows (including attachment signed URLs), included in `latest_notice`, and deletable from admin notice list via board-notice id routing + board attachment cleanup | supabase/functions/fc-notify/index.ts, AGENTS.md | deploy verified (`supabase functions deploy fc-notify --project-ref ubeginyxaotcamuqpmud`; `supabase functions list` shows `fc-notify` ACTIVE v51 @ 2026-02-25 06:42:37 UTC); lint attempt noted (`npm run lint -- supabase/functions/fc-notify/index.ts` fails only on expected Deno remote import resolution under Expo ESLint) | On device, verify board `공지` write appears in `/notice` and home latest notice, and admin delete from notice list removes corresponding board post`
- `2026-02-25 | Mobile Chat Name Resolution + Message URL Naming | Fixed admin chat header/sender label fallback that showed generic `FC` instead of actual FC name by (1) resolving admin chat peer name from `fc_profiles` when route param is missing/generic, (2) sending FC-originated message notification URLs with encoded `targetName` from sender display name, (3) extending `fc-notify` message-to-admin default URL generation to resolve sender name from payload or `fc_profiles`, and (4) hardening admin messenger list item name fallback to phone/FC when profile name is blank | app/chat.tsx, app/admin-messenger.tsx, supabase/functions/fc-notify/index.ts, AGENTS.md | mobile lint pass (`npm run lint -- app/chat.tsx app/admin-messenger.tsx`) | Deploy `fc-notify` function and verify: admin opens chat from push -> header shows FC name, not generic `FC`; messenger list search/render remains stable for blank-name legacy rows`
- `2026-02-25 | Mobile Admin Navigation Consistency (Notice/Board) | Resolved admin/manager bottom-nav confusion by fixing board-manage home-tab routing (removed history-back behavior), unifying admin notice tab destination to notice list (`/notice`) across shared/custom nav paths, enabling admin onboarding/exam tabs to navigate to home with explicit mode query (`/?mode=onboarding|exam`), and aligning home-screen admin nav item count to include notice for consistent tab structure across screens | components/BottomNavigation.tsx, app/admin-board-manage.tsx, app/index.tsx, AGENTS.md | mobile lint pass (`npm run lint -- components/BottomNavigation.tsx app/index.tsx app/admin-board-manage.tsx app/notice.tsx app/settings.tsx`) | On device, verify admin board-manage home tab always opens home (not previous notice), admin tab count remains consistent across home/notice/board, and onboarding/exam tabs on notice/settings route to the correct home mode`
- `2026-02-25 | Release/Build Tooling | Resolved recurring EAS upload failure (`git clone ... exit 128`) at root by replacing direct Husky `prepare` install with guarded prepare script that removes local `.husky` `core.hooksPath` by default, and by adding EAS build wrapper scripts that auto-clear conflicting local hooks path before invoking `eas build` | scripts/prepare.js, scripts/eas-build.js, package.json, docs/guides/COMMANDS.md, docs/guides/명령어 모음집.txt, AGENTS.md | local repro check: before fix `git clone --depth 1 file:///...` failed with clone-protection on local `core.hooksPath`; after fix hooksPath cleared and wrapper command path available; docs command paths updated to wrapper scripts | Use `npm run eas:build:android` / `npm run eas:build:ios` for production builds and only enable Husky locally when explicitly needed`
- `2026-02-24 | Board Attachment Sign 400 Recovery + Error Message Fallback | Resolved board post-create failure path caused by production drift on attachment sign limits by deploying `board-attachment-sign` with shared 20-file limit (`_shared/board.ts`), and hardened mobile/web board API invoke error handling to surface function JSON error message (with 400 fallback guidance) instead of raw generic `FunctionsHttpError` text | supabase/functions/board-attachment-sign/index.ts, supabase/functions/_shared/board.ts, lib/board-api.ts, web/src/lib/board-api.ts, AGENTS.md | deploy state verified (`supabase functions list --project-ref ubeginyxaotcamuqpmud`: `board-attachment-sign` ACTIVE v12 @ 2026-02-24 16:06:05 UTC); lint pass (`npm run lint -- lib/board-api.ts`, `cd web && npm run lint -- src/lib/board-api.ts`) | Re-test mobile admin board create/edit with 6~20 attachments and confirm user-facing failure text is specific when limits are exceeded`
- `2026-02-24 | Attachment List Scroll vs Drag Gesture Tuning | Fixed compose/edit attachment area scroll lock caused by drag-gesture capture: removed row-level long-press drag trigger from image rows, limited drag start to right-side move handle long-press, and increased draggable activation distance so parent form scroll remains responsive on touch/drag starts over image rows | app/admin-board.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/admin-board.tsx`) | On Android/iOS drag on list body should scroll page; long-press move icon should still reorder image items`
- `2026-02-24 | Image Viewer Swipe/Pan Gesture Conflict Fix | Restored horizontal image paging in preview modal by enabling image pan gesture only while zoomed (`Pan.enabled(isZoomed)`), preventing one-finger swipe conflicts with parent FlatList when scale is 1x | components/ImagePreviewModal.tsx, AGENTS.md | mobile lint pass (`npm run lint -- components/ImagePreviewModal.tsx`) | Verify at 1x swipe left/right changes image, and after pinch-zoom pan remains available until zoom reset`
- `2026-02-24 | Image Viewer Pinch Crash Hotfix | Fixed Android hard crash during pinch-zoom in image preview modal by converting shared `clamp` helper to worklet-safe function (`'worklet'` directive) so gesture callbacks no longer synchronously invoke non-worklet JS on UI thread | components/ImagePreviewModal.tsx, AGENTS.md | mobile lint pass (`npm run lint -- components/ImagePreviewModal.tsx`) | Re-test pinch-zoom on Android image viewer to confirm no forced app termination`
- `2026-02-24 | Board List 4th Image Preview Hotfix Deploy | Deployed updated `board-list` Edge Function to production (`ubeginyxaotcamuqpmud`) and re-verified target post (`설계 의뢰 페이지 안내`) now returns signed URLs for all 4 image attachments (previously 4th missing signedUrl in list payload) | supabase/functions/board-list/index.ts, AGENTS.md | `supabase functions deploy board-list --project-ref ubeginyxaotcamuqpmud`; direct function invoke check confirms image #1~#4 `signedUrl=true` | Ask operator to hard refresh app session and confirm 4th thumbnail renders without placeholder`
- `2026-02-24 | Image Viewer Zoom Gesture Recovery | Fixed non-working image zoom in mobile preview modal by wrapping modal content with `GestureHandlerRootView` (Android modal gesture root separation) and hardening pinch gesture zoom-state sync on begin/end so list paging stops immediately during pinch and resumes correctly after zoom reset | components/ImagePreviewModal.tsx, AGENTS.md | mobile lint pass (`npm run lint -- components/ImagePreviewModal.tsx`) | On Android/iOS open image viewer and verify two-finger pinch zoom + pan work reliably in modal`
- `2026-02-24 | Mobile Board Drag Reorder + 4th Thumbnail Fallback | Replaced admin board image ordering UX with true long-press drag-and-drop (image sections only) using `react-native-draggable-flatlist`, keeping non-image files non-reorderable; hardened list thumbnail rendering to always render up to 4 image slots with per-thumb load-failure fallback; and updated `board-list` signing to retry image signed-URL generation and retain image rows even when signing fails so preview slots don’t silently disappear | app/admin-board.tsx, app/board.tsx, app/admin-board-manage.tsx, supabase/functions/board-list/index.ts, package.json, package-lock.json, AGENTS.md | mobile lint pass (`npm run lint -- app/admin-board.tsx app/board.tsx app/admin-board-manage.tsx`) | Deploy `board-list` function, then verify on Android/iOS that image drag reorder works and 4-image preview no longer collapses to 3`
- `2026-02-24 | Mobile Board Attachment Type Separation + Image Long-Press Reorder Completion | Refactored admin board compose/edit attachment UI to render images and non-image files in separate sections (existing/new each), normalized local attachment state to image-first grouping, and completed image reorder interaction by enabling long-press reorder menu directly on image rows (plus move handle fallback) while excluding file rows from reorder controls | app/admin-board.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/admin-board.tsx`) | On Android/iOS compose/edit screen, verify images/files are split, long-press image row reorders, and file rows never show reorder action`
- `2026-02-24 | Mobile Board Attachment Reorder Scope/Trigger Fix | Fixed non-working long-press reorder in admin board compose/edit by moving reorder trigger to a dedicated image-handle button (avoids row press gesture conflicts) and restricting reorder logic to image attachments only; non-image files are excluded from ordering controls and remain in place | app/admin-board.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/admin-board.tsx`) | On Android/iOS edit screen, long-press image handle to reorder and verify non-image file rows have no reorder action`
- `2026-02-24 | Image Viewer Pinch-Zoom Support | Added pinch-to-zoom + pan support to mobile image viewer (`ImagePreviewModal`) using gesture-handler/reanimated; zoom range 1x~4x, auto-reset to base position when zoom returns near 1x, and list swipe is disabled while zoomed to avoid gesture conflicts | components/ImagePreviewModal.tsx, AGENTS.md | mobile lint pass (`npm run lint -- components/ImagePreviewModal.tsx`) | On Android/iOS, open viewer and verify pinch zoom in/out, drag pan while zoomed, and horizontal image swipe recovery after resetting zoom`
- `2026-02-24 | Image Viewer Index Label Visibility | Updated mobile image viewer index label to always display explicit progress text (`총 N장 중 M번째`) in header, including single-image cases (`총 1장 중 1번째`) for clear current/total awareness | components/ImagePreviewModal.tsx, AGENTS.md | mobile lint pass (`npm run lint -- components/ImagePreviewModal.tsx`) | Verify viewer header text updates correctly while swiping across multi-image posts`
- `2026-02-24 | Image Viewer Horizontal Crop Fix | Fixed mobile full-screen image viewer horizontal clipping by aligning swipe page width with actual modal viewport width (`windowWidth - overlay horizontal padding`) for offset calc, paging layout, and index tracking; resolves left/right cut-off when previewing landscape or wide images | components/ImagePreviewModal.tsx, AGENTS.md | mobile lint pass (`npm run lint -- components/ImagePreviewModal.tsx`) | On Android/iOS, open image viewer and verify no horizontal cut-off across swipe pages`
- `2026-02-24 | Board List 4th Thumbnail Reliability | Fixed board list thumbnail fallback issue where the 4th slot could show placeholder instead of a real image: changed `board-list` signing loop to count only successfully signed image URLs toward the 4-image cap and filtered mobile list preview rendering to signed-image items only | supabase/functions/board-list/index.ts, app/board.tsx, app/admin-board-manage.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/board.tsx app/admin-board-manage.tsx`) | Deploy `board-list` function and verify posts with 4+ images render 4 real thumbnails in list cards`
- `2026-02-24 | Board Attachment Reorder + Edit Header Action + 4-Image Preview | Added long-press attachment order control in mobile board compose/edit (`위/아래/맨앞/맨뒤`) for both existing and new files, persisted edited existing-attachment order through `board-update` (`attachmentOrder`) and new-file order through `board-attachment-finalize` (`sortOrder`), added top-right `게시글 수정` action button on edit screen, and expanded board list preview image capacity from 3 to 4 (client slice + server signed-image cap) | app/admin-board.tsx, lib/board-api.ts, web/src/lib/board-api.ts, supabase/functions/board-update/index.ts, supabase/functions/board-attachment-finalize/index.ts, supabase/functions/board-detail/index.ts, supabase/functions/board-list/index.ts, app/board.tsx, app/admin-board-manage.tsx, supabase/schema.sql, supabase/migrations/20260224_add_board_attachments_sort_order.sql, AGENTS.md | mobile lint pass (`npm run lint -- app/admin-board.tsx app/board.tsx app/admin-board-manage.tsx app/board-detail.tsx lib/board-api.ts components/ImagePreviewModal.tsx`); web lint pass (`cd web && npm run lint -- src/lib/board-api.ts src/app/dashboard/board/page.tsx`) | Apply DB migration (`supabase db push`) and deploy impacted functions (`board-update`, `board-attachment-finalize`, `board-detail`, `board-list`) before production validation`
- `2026-02-24 | Mobile Board Image Viewer Swipe Navigation | Upgraded board image preview from single-image modal to swipeable gallery: `ImagePreviewModal` now accepts image arrays + initial index and supports horizontal paging with current index indicator (`n / total`); wired board/detail/admin manage screens to open preview with full attachment image set at tapped image index | components/ImagePreviewModal.tsx, app/board.tsx, app/admin-board-manage.tsx, app/board-detail.tsx, AGENTS.md | mobile lint pass (`npm run lint -- components/ImagePreviewModal.tsx app/board.tsx app/admin-board-manage.tsx app/board-detail.tsx`) | On Android/iOS, open a post with 2+ images and verify left/right swipe, index indicator, close/backdrop behavior`
- `2026-02-24 | Mobile Board Image Detail View | Added in-app full-screen image preview (tap-to-open) for board post attachments so uploaded images can be viewed in detail without leaving the app: applied to FC board modal, admin board manage modal, and board-detail attachment list via shared `ImagePreviewModal` component; closes on backdrop/X with safe state reset when post modal closes | components/ImagePreviewModal.tsx, app/board.tsx, app/admin-board-manage.tsx, app/board-detail.tsx, AGENTS.md | mobile lint pass (`npm run lint -- app/board.tsx app/admin-board-manage.tsx app/board-detail.tsx components/ImagePreviewModal.tsx`) | On Android/iOS, open a post with image attachments and verify tap -> full-screen preview -> close -> reopen other images`
- `2026-02-24 | Mobile Board Link/Selection UX + Attachment Limit + Mutation Stability | Updated mobile board surfaces to render post/comment text as selectable link-aware text (URL auto-style blue+underline with open/copy/select options), raised board attachment limit from 5 to 20 (mobile form + shared board edge-function limits), and hardened create/edit/delete-adjacent flows by null-safe text rendering and deterministic compose completion routing (`router.replace('/admin-board-manage')`) to avoid runtime exits on completion | components/LinkifiedSelectableText.tsx, app/board.tsx, app/admin-board-manage.tsx, app/board-detail.tsx, app/admin-board.tsx, supabase/functions/_shared/board.ts, web/src/app/dashboard/board/page.tsx, package.json, package-lock.json, AGENTS.md | mobile lint pass (`npm run lint -- app/admin-board.tsx app/board.tsx app/admin-board-manage.tsx app/board-detail.tsx components/LinkifiedSelectableText.tsx`); web lint pass (`cd web && npm run lint -- src/app/dashboard/board/page.tsx`) | Deploy updated board edge function(s) that import `_shared/board.ts` (at minimum `board-attachment-sign`) and run on-device create/edit/delete + URL action checks`
- `2026-02-24 | Web Push Service Worker Delivery Fix | Fixed desktop notification non-display path by: (1) allowing static asset and service worker requests through middleware matcher, (2) hardening `sw.js` push payload parsing with text fallback and immediate activate/claim, (3) switching dashboard test notification icon/badge paths from missing `/favicon.png` to existing `/favicon.ico`; redeployed admin web and re-verified end-to-end send chain | web/middleware.ts, web/public/sw.js, web/src/app/dashboard/page.tsx, AGENTS.md | web lint pass (`cd web && npm run lint -- middleware.ts src/app/dashboard/page.tsx public/sw.js`); Vercel prod deploy complete (`admin-ff5m38mw8-jun-jeongs-projects.vercel.app`, alias `adminweb-red`); `curl -I https://adminweb-red.vercel.app/sw.js` returns 200; direct POST `https://adminweb-red.vercel.app/api/fc-notify` returns `web_push: { ok: true, status: 200, sent: 1, failed: 0 }` | In subscribed admin browser: hard refresh (`Ctrl+F5`) once to update SW, click `알림 테스트`; if still hidden, enable Windows Chrome app notifications/focus policy`
- `2026-02-24 | Push Diagnostics Visibility | Added end-to-end push diagnostics to isolate “no desktop notification” issues: (1) fc-notify now returns `web_push` callback result (`ok/status/sent/failed/reason`) for admin-targeted notify/message/update flows, (2) dashboard header now includes `알림 테스트` button to trigger immediate browser/OS notification via Notification API + service worker, enabling client-side vs server-side split checks | supabase/functions/fc-notify/index.ts, web/src/app/dashboard/page.tsx, AGENTS.md | web lint pass (`cd web && npm run lint -- src/app/dashboard/page.tsx`); `supabase functions deploy fc-notify --project-ref ubeginyxaotcamuqpmud` complete (version 49); direct fc-notify invoke returns `web_push: { ok: true, status: 200, sent: 1, failed: 0 }`; Vercel deploy ready (`admin-fqbyh32rq-jun-jeongs-projects.vercel.app`, alias `adminweb-red`) | Ask operator to press dashboard `알림 테스트`; if no toast appears, check OS/browser notification settings (Focus assist / site permission / quiet notification UI)`
- `2026-02-24 | Dashboard Notification Button | Added web notification setup button directly on admin dashboard header (next to refresh) so operators can request permission and force re-register browser push subscription without leaving dashboard; wired button to shared `registerWebPushSubscription` helper with success/failure user feedback | web/src/app/dashboard/page.tsx, AGENTS.md | web lint pass (`cd web && npm run lint -- src/app/dashboard/page.tsx`); Vercel production deploy complete (`admin-q7k45zhrs-jun-jeongs-projects.vercel.app`, alias `adminweb-red`) | Verify header button triggers browser permission flow + FC->admin push delivery`
- `2026-02-24 | Web Push Permission UX | Added explicit web notification permission control in dashboard settings: status indicator (`granted/denied/default/unsupported`) + action button to request permission and force re-register web push subscription; refactored WebPushRegistrar registration flow into reusable client helper used by both auto-registrar and manual settings action | web/src/components/WebPushRegistrar.tsx, web/src/app/dashboard/settings/page.tsx, AGENTS.md | web lint pass (`cd web && npm run lint -- src/components/WebPushRegistrar.tsx src/app/dashboard/settings/page.tsx`); Vercel production deploy complete (`admin-nbmu1fo8i-jun-jeongs-projects.vercel.app`, alias `adminweb-red`) | Re-verify admin browser permission/registration from settings page and confirm live FC->admin message push`
- `2026-02-24 | Admin Web Push Delivery Recovery | Resolved admin web push non-delivery root cause: auth token comparisons failed due malformed env/header formatting (literal \n/newline/quote whitespace). Hardened `/api/admin/push` token normalization (`X-Admin-Push-Secret`/Bearer/apikey), added VAPID env normalization in `sendWebPush`, refreshed Vercel production env variables, and redeployed admin web so `adminweb-red` now returns 200 (`sent:1`) on both secret and service-role auth paths | web/src/app/api/admin/push/route.ts, web/src/lib/web-push.ts, AGENTS.md | web lint pass; Vercel prod deploy complete (`admin-c87og339h-jun-jeongs-projects.vercel.app` alias `adminweb-red`); direct `/api/admin/push` probe returns 200 on both auth modes; direct `fc-notify` invoke returns 200 and writes admin notification row | Re-test real FC app -> admin chat message push on the subscribed admin browser session (same device/profile with notification permission granted)`
- `2026-02-24 | Web Push VAPID Normalization | Hardened web push config parsing to normalize malformed env values (trim quotes/whitespace/remove literal \n) and fail with explicit logs on invalid VAPID settings, preventing silent push drop due env formatting errors | web/src/lib/web-push.ts, AGENTS.md | web lint pass (`cd web && npm run lint -- src/lib/web-push.ts src/app/api/admin/push/route.ts`) | Redeploy web so normalized VAPID parsing is applied in production runtime`
- `2026-02-24 | Admin Web Push Callback Reliability | Fixed app->admin web push callback hardening by normalizing ADMIN_WEB_URL to origin(`/api/admin/push`), adding callback non-2xx logging in fc-notify, and allowing /api/admin/push auth via either X-Admin-Push-Secret or service-role bearer fallback to reduce secret-drift failures | supabase/functions/fc-notify/index.ts, web/src/app/api/admin/push/route.ts, AGENTS.md | web lint pass (`cd web && npm run lint -- src/app/api/admin/push/route.ts`); local env key presence check (`web/.env.local`); `supabase functions deploy fc-notify --project-ref ubeginyxaotcamuqpmud` complete (version 48) | Ensure deployed Next runtime and Supabase secrets keep `ADMIN_PUSH_SECRET`/service-role auth path consistent; verify live app/web push flow`
- `2026-02-24 | Admin Web Push Notifications | 어드민 브라우저(백그라운드 포함) 웹 푸시 알림 추가. FC 서류 제출·동의서 입력·시험 신청·위촉 날짜 등 모든 FC 액션 시 어드민 크롬/엣지 알림 수신. 구현: (1) /api/admin/push Next.js 엔드포인트 신규 생성(ADMIN_PUSH_SECRET 검증 후 admin web_push_subscriptions에 sendWebPush), (2) fc-notify Edge Function에 notifyAdminWebPush 헬퍼 추가 → notify/message/fc_update/fc_delete 타입에서 admin 대상 시 ADMIN_WEB_URL/api/admin/push 콜백, (3) /api/fc-notify Next.js 라우트에 notify+target_role=admin 케이스 web push 처리 추가 | web/src/app/api/admin/push/route.ts, supabase/functions/fc-notify/index.ts, web/src/app/api/fc-notify/route.ts, AGENTS.md | web lint pass | Supabase 시크릿에 ADMIN_PUSH_SECRET, ADMIN_WEB_URL 등록 필요; Next.js .env.local에 ADMIN_PUSH_SECRET 추가 필요; supabase functions deploy fc-notify 배포 필요`
- `2026-02-20 | Estimation Input Pack | Added a price-free outsourcing estimation input document that consolidates objective project scope, architecture, code size metrics, security constraints, deployment/operations context, and vendor questionnaire items for cost decision making | docs/guides/OUTSOURCING_ESTIMATION_INPUT_PACK.md, AGENTS.md | Manual cross-check against README/AGENTS/contracts/schema/scripts and repository scan metrics | Keep this pack updated when scope, role rules, schema, or deployment model changes`
- `2026-02-20 | Docs README Refresh | Rewrote root README to reflect current dual-platform architecture, auth/session model, role/status workflow, security guardrails, and operational commands from AGENTS/project code | README.md, AGENTS.md | Manual doc consistency review against AGENTS.md, package scripts, and key route/API files | Keep README aligned when role permissions or workflow states change`
- `2026-02-20 | Mobile Exam Apply Pull-to-Refresh Fix | Fixed non-working pull-to-refresh gesture on life/nonlife exam apply screens by removing nested inner ScrollView and wiring RefreshControl directly to KeyboardAwareWrapper (single scroll owner), while keeping unified full-refresh handler logic | app/exam-apply.tsx, app/exam-apply2.tsx, AGENTS.md | npm run lint -- app/exam-apply.tsx app/exam-apply2.tsx | Re-test drag-down refresh gesture on Android device for both screens`
- `2026-02-20 | Mobile Exam Apply Refresh UX | Standardized pull-to-refresh behavior across life/nonlife exam apply screens so drag-refresh and header refresh both trigger full data refresh (round list + my applications + profile allowance state) instead of rounds-only refresh | app/exam-apply.tsx, app/exam-apply2.tsx, AGENTS.md | npm run lint -- app/exam-apply.tsx app/exam-apply2.tsx | Verify on-device pull-down refresh updates status card and schedule list together`
- `2026-02-20 | Mobile Exam Apply Filtering | Unified life/nonlife exam-apply round visibility criteria to show only schedules whose registration deadline is within the last 7 days, and normalized both queries to `YYYY-MM-DD` cutoff comparison for stable date filtering | app/exam-apply.tsx, app/exam-apply2.tsx, AGENTS.md | npm run lint -- app/exam-apply.tsx app/exam-apply2.tsx | Validate both exam apply screens hide older closed rounds consistently on device`
- `2026-02-20 | Affiliation Data Normalization | Added one-time Supabase data migration to convert legacy `1본부` labels to new `1팀~8팀` org labels and hardened mobile FC profile load path to normalize legacy affiliation strings into current options | supabase/migrations/20260220_normalize_fc_affiliation_labels.sql, app/fc/new.tsx, AGENTS.md | npm run lint -- app/fc/new.tsx app/signup.tsx | Run migration in linked Supabase project and spot-check historical fc_profiles affiliation values`
- `2026-02-20 | Mobile Affiliation Options | Updated all app affiliation option labels to new team/region/manager mapping (1팀~8팀) in signup and FC registration flows so selectable affiliation text is consistent with latest org structure | app/signup.tsx, app/fc/new.tsx, AGENTS.md | npm run lint -- app/signup.tsx app/fc/new.tsx | If historical profiles still store old labels, plan one-time data normalization policy`
- `2026-02-20 | Mobile Notifications UX | Removed temporary in-app drag debug instrumentation (overlay panel + debug event logging) after stabilizing long-press multi-select behavior; kept production drag select/deselect + auto-scroll logic only | app/notifications.tsx, AGENTS.md | npm run lint -- app/notifications.tsx | Validate selection flow on device without debug overlay/log noise`
- `2026-02-20 | Mobile Notifications UX | Updated drag toggle behavior to allow same-gesture re-entry toggling (removed one-pass visited guard), so moving back over already-touched rows in a single drag flips selection state again (e.g., A,B,C then back over B,C deselects) while preserving auto-scroll | app/notifications.tsx, AGENTS.md | npm run lint -- app/notifications.tsx | Verify back-and-forth drag toggles same rows repeatedly without lifting finger`
- `2026-02-20 | Mobile Notifications UX | Switched drag multi-select from fixed action mode to per-item dynamic toggle so a single drag in selection mode can both select and deselect without finger reset; kept visited-once-per-drag guard and auto-scroll support | app/notifications.tsx, AGENTS.md | npm run lint -- app/notifications.tsx | Validate single continuous drag toggles mixed selected/unselected rows while preserving auto-scroll behavior`
- `2026-02-20 | Mobile Notifications UX | Added edge-triggered auto-scroll during long-press drag multi-select so dragging near top/bottom continuously scrolls and keeps selecting/deselecting across off-screen rows; wired FlatList/content height refs and drag-pointer loop with safe stop conditions | app/notifications.tsx, AGENTS.md | npm run lint -- app/notifications.tsx | Validate sustained drag-to-bottom/top selection on Android and iOS with long lists`
- `2026-02-20 | Mobile Notifications UX | Completed long-press multi-select polish by adding drag deselect mode (long-press on selected item => deselect sweep), stabilizing drag visited-state handling, and unifying selected-row visuals (card border/background/check badge/text accents) while preserving in-app drag debug telemetry | app/notifications.tsx, AGENTS.md | npm run lint -- app/notifications.tsx | Reproduce select/deselect drag sweeps on Android and confirm consistent selected-card visuals across mixed rows`
- `2026-02-19 | Manager Notice Permissions | 본부장(manager)이 공지사항을 등록할 수 있고 본인 게시글에 한해 수정·삭제 가능하도록 변경. notices 테이블에 created_by 컬럼 추가, API 소유권 검증(DELETE/PATCH), 목록·상세·등록·수정 페이지 UI 반영 | supabase/schema.sql, supabase/migrations/20260219_add_notices_created_by.sql, web/src/app/api/admin/notices/route.ts, web/src/app/dashboard/notifications/actions.ts, web/src/app/dashboard/notifications/page.tsx, web/src/app/dashboard/notifications/create/page.tsx, web/src/app/dashboard/notifications/[id]/page.tsx, web/src/app/dashboard/notifications/[id]/edit/page.tsx | web lint pass | Supabase migration 배포 필요 (created_by 컬럼); admin은 여전히 전체 공지 관리 가능`
- `2026-02-19 | Web Docs Badge Counting | Updated dashboard "검토 중" doc badge to count only submitted docs that are neither approved nor rejected | web/src/app/dashboard/page.tsx | web lint | Keep doc summary badges aligned with submitted-doc filters`
- `2026-02-19 | Web Profile Editability | Fixed FC profile detail/edit + admin memo save to use server-mediated admin API (`/api/admin/fc`) and applied admin-only editable controls (manager read-only) | web/src/app/dashboard/profile/[id]/page.tsx | web lint | Extend same server-mediated write pattern to remaining direct client-write screens`
- `2026-02-19 | Resident Number Visibility | Expanded full resident-number rendering paths by removing masked fallbacks and wiring admin screens (web/mobile) to resident-number retrieval action | supabase/functions/admin-action/index.ts, web/src/app/dashboard/exam/applicants/page.tsx, web/src/app/dashboard/profile/[id]/page.tsx, app/exam-manage.tsx, app/exam-manage2.tsx, app/fc/new.tsx | mobile/web lint + flow-path review | Add focused authorization regression checks for resident-number retrieval action`
- `2026-02-19 | Governance/Docs | Removed WORK_LOG recent-table row-cap enforcement from policy and CI checks to allow continuous history accumulation | AGENTS.md, .claude/PROJECT_GUIDE.md, scripts/ci/check-governance.mjs | Governance script review | Keep anchor-link integrity checks while logs grow`
- `2026-02-19 | Web Docs UX | Fixed admin docs tab so "submitted documents" list/count includes only rows with real uploaded storage paths (exclude empty path placeholders) | web/src/app/dashboard/page.tsx | web lint + condition-path review | Add regression check for docs request rows with empty storage_path`
- `2026-02-19 | Web Profile/PII | Wired full resident-number display on FC detail page for admin via server-mediated resident-number API with masked fallback | web/src/app/dashboard/profile/[id]/page.tsx | web lint + route contract review | Keep manager/read-only flows on masked fallback and monitor API failure visibility`
- `2026-02-19 | Auth/Consent Guard | Blocked allowance consent before temp ID and blocked FC set-password when admin/manager phone exists; aligned Deno editor setup | app/consent.tsx, supabase/functions/fc-consent/index.ts, supabase/functions/set-password/index.ts, supabase/functions/deno.json | Diff review + governance doc sync | Add regression checks for temp-id gate and cross-role phone collisions`
- `2026-02-11 | Release/Build | Applied Android release minify and shrink resources settings | app.json | Config reviewed in repo | Validate release pipeline on next build`
- `2026-02-11 | Security/Governance | Removed sensitive local configs and non-source test artifacts from tracking | .gitignore, testsprite_tests/* | Git status and policy checks | Keep local-secret hygiene rules enforced`
- `2026-02-11 | Stability Cleanup | Removed unneeded build bundles and unreferenced modules | dist-web-new2/*, app/admin-register.tsx | Repo scan and dependency checks | Watch for accidental artifact reintroduction`
- `2026-02-11 | Notifications | Fixed app notification center persistence path (`fc-notify`) and linked delivery flow | supabase/functions/fc-notify/index.ts, web/src/app/actions.ts | Functional flow reviewed | Add regression checks for push + stored notice parity`
- `2026-02-11 | Exams | Normalized exam registration deadline handling to end-of-day semantics | app/exam-apply.tsx, web/src/app/dashboard/exam/schedule/page.tsx | Cross-view behavior check | Keep date rule consistency in API/server paths`
- `2026-02-11 | Docs Deadline | Corrected docs deadline reminder timing and admin API alignment | supabase/functions/docs-deadline-reminder/index.ts, web/src/app/api/admin/fc/route.ts | Logic-path review | Add alert coverage for reminder edge times`
- `2026-02-11 | Notices | Routed web notices through server API for RLS-safe access | web/src/app/api/admin/notices/route.ts | Endpoint behavior verified | Keep notice access server-mediated`
- `2026-02-10 | Web Runtime | Mitigated Windows Next.js dev lockfile conflicts | web/scripts/clean-next.mjs | Local dev command validation | Monitor for dev startup regressions`
- `2026-02-10 | Auth Session | Stabilized web session cookie sync and API auth reliability | web/src/hooks/use-session.tsx | Dashboard auth flow validation | Continue auth edge-case hardening`
- `2026-02-10 | Applicants UX | Improved exam applicants UI typing and resident-number display handling | web/src/app/dashboard/exam/applicants/page.tsx | UI and type checks | Keep PII display policy consistent`

## Golden Rules

### Immutable
1. Never store resident number plaintext in database, logs, or client payloads.
2. Keep admin write operations behind trusted server paths (Edge Functions/service-role context), not direct anon client writes.
3. Preserve role model behavior: `admin` write, `manager` read-only, `fc` self-service.
4. When schema changes, update both `supabase/schema.sql` and matching `supabase/migrations/*.sql`.
5. Preserve existing Edge Function response contract (`ok`, `message`) unless migration is explicitly coordinated.

### Do's
- Use `use-session` state (`role`, `residentId`, read-only flags) as the app authorization source.
- Reuse existing shared modules before adding new utilities (`lib/*`, `components/*`, `hooks/*`).
- Use TanStack Query patterns (`useQuery` for reads, `useMutation` + invalidation for writes).
- Keep route/file conventions of Expo Router and Next App Router.
- Add/update docs when behavior or rules change (`.claude/PROJECT_GUIDE.md`, `.claude/WORK_LOG.md`, `.claude/WORK_DETAIL.md`).

### Don'ts
- Do not hardcode keys, tokens, or service credentials.
- Do not bypass RLS constraints by adding unsafe client-side privileged writes.
- Do not break status progression or redefine existing status strings without migration planning.
- Do not add manager write paths in UI or API.
- Do not ship schema-only or code-only changes when both layers are required.

## Standards & References

### Coding Standards
- TypeScript strictness first; avoid broad `any`.
- Handle unknown errors defensively (`unknown` -> `Error` narrowing).
- Keep business logic centralized and avoid duplicate workflow logic across mobile/web.
- Prefer small, composable modules and explicit types in shared contracts (`types/*`, `contracts/*`).

### Git Strategy and Commit Format
- Branch from latest mainline and keep changes scoped by domain.
- Use conventional commit style observed in repository history:
  - `feat(scope): ...`
  - `fix(scope): ...`
  - `chore(scope): ...`
  - `docs(scope): ...`
- Keep each commit buildable and logically atomic.

### References
- Primary policy: `.claude/PROJECT_GUIDE.md`
- Work logs: `.claude/WORK_LOG.md`, `.claude/WORK_DETAIL.md`
- Commands: `docs/guides/COMMANDS.md`
- Contracts: `contracts/database-schema.md`, `contracts/api-contracts.md`, `contracts/component-contracts.md`
- Architecture decisions: `adr/README.md`
- Legacy AI context doc: `CLAUDE.md` (optional deep reference)

### Maintenance Policy
- If implementation and AGENTS rules diverge, update AGENTS and linked docs in the same change set.
- If a new high-context zone appears (new framework, new package boundary, or dense domain module), add a local `AGENTS.md`.
- Prefer explicit rule updates over ad-hoc comments in PRs.

## Context Map (Action-Based Routing)
- **[Mobile screens and navigation (Expo Router)](./app/AGENTS.md)** - Use when editing `app/*` routes, navigation stacks, or FC/admin mobile flows.
- **[Mobile shared UI components](./components/AGENTS.md)** - Use when changing reusable React Native UI, status controls, headers, or shared visual patterns.
- **[Mobile session and behavior hooks](./hooks/AGENTS.md)** - Use when modifying auth/session, identity gates, or platform-specific hook logic.
- **[Admin web application (Next.js)](./web/AGENTS.md)** - Use when working in `web/*`, including dashboard pages, route handlers, and manager read-only behavior.
- **[Supabase schema and backend policy](./supabase/AGENTS.md)** - Use when changing DB schema, migrations, storage/RLS, or backend data contracts.
- **[Supabase Edge Functions implementation](./supabase/functions/AGENTS.md)** - Use when implementing or updating Deno function handlers and shared function utilities.
